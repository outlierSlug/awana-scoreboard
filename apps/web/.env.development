# Local development defaults. Committed on purpose: these are not secrets, and
# every developer needs them for `npm run dev` to work at all.
#
# To override without editing this file, create .env.development.local, which
# git ignores.

VITE_API_BASE_URL=http://localhost:5201
